## Sample application to demostrate phpUnit

### Installation

* Clone the repository
* Run composer install from the root of the project

### Running tests

* From the root of the project type the following command:
```` vendor/bin/phpunit ````

